{
  "name": "wauth",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "sdk",
    "frontend",
    "worker"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "setup": "bun install && bun run setup:db"
  }
}